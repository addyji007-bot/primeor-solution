# Copy this file to .env and fill in your actual local MySQL credentials.
# .env itself is git-ignored and should NEVER be committed.

DB_HOST=localhost
DB_PORT=3306
DB_NAME=Primeor_Sol
DB_USER=root
DB_PASSWORD=your_password_here
